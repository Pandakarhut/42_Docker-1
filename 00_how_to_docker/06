docker run -it --rm debian
# Then in container run: apt update -y && apt upgrade -y && apt install git gcc nano -y

# Example C file (create file.c):

# include <stdio.h>
# int main()
# {
#   printf("testing c\n");
#   return(0);
# }

# Compile: gcc file.c
# Run: ./a.out
